version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: opentab/Dockerfile
    ports:
      - "8001:8001"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn opentab.main:app --reload --host 0.0.0.0 --port 8001
    working_dir: /app

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://backend:8001
    command: npm run dev -- --host 0.0.0.0
    depends_on:
      - backend
